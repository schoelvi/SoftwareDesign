//import { Person } from "../classes/Person";

/*describe("Check person class", () => {
  let name : string = "Christoph Fleig";
  let age : number = 31;
  let personA = new Person(name, age);
  
  test("Test 1: Check constructor of Person class", () => {
    expect((typeof personA.getName()) == "string").toBeTruthy();
    expect((typeof personA.getAge()) == "number").toBeTruthy();
  });

  test("Test 2: Check values", () => {
    expect(personA.getName()).toBe(name);
    expect(personA.getAge()).toBe(age);
  });

  test("Test 3: Validate age", () => {
    expect(personA.validateAge(personA.getAge().toString())).toBeTruthy();
    expect(personA.validateAge("0021")).toBeFalsy();
  });
});*/